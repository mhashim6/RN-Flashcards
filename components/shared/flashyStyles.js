import { StyleSheet } from "react-native";

const FlashyStyles = StyleSheet.create({
    roundedBorder: {
        borderColor: 'grey',
        borderWidth: 1,
        borderRadius: 4,
    },
    textInput: {
        fontSize: 20,
        padding: 8,
    }
})

export default FlashyStyles